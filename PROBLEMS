List of known issues:
=====================

* OSX: macOS `brew` installed modules are not compiled correctly and will crash the project ( BlackMarlinExec )
* GUI && CLI ( one way : TODO )
* Collectors : need power-shellscripts
* Sharp-Return-shell